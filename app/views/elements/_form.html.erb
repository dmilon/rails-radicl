<%= simple_form_for [@zone, @element] do |f| %>
  <%= f.input :name, required: true %>
  <%= f.input :quantity, as: :integer, required: true %>
  <%= f.input :photo, required: false %>
  <%= f.submit 'go!', class: "btn btn-home btn-lg" %>
<% end %>
