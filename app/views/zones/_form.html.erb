<%= simple_form_for [@garden, @zone] do |f| %>
  <%= f.input :name %>
  <%= f.input :area %>
  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.submit 'go!', class: "btn btn-home btn-lg" %>
<% end %>
