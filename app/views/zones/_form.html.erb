<%= simple_form_for [zone] do |f| %>
  <%= f.input :name %>
  <%= f.input :area, placeholder: "for ex: 1000m2" %>
  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.submit 'go!', class: "btn btn-home btn-lg" %>
<% end %>
